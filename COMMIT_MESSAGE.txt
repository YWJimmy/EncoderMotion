fix(build): split mixed-port USER_IO SysConfig instance

- separate OLED, select button, and confirm button GPIO instances
- use generated per-instance PORT and PIN macros
- remove all references to nonexistent USER_IO_PORT
- add a regression build check without the invalid shared-port macro
