fix(encoder): stabilise quadrature counting and unify motion timing

- commit encoder position only after a complete ordered AB cycle
- preserve four-times count calibration by committing four counts per cycle
- cancel stationary single-edge bounce inside the transition accumulator
- atomically capture left and right count and delta values
- drain newly pending GPIO edges inside the encoder ISR
- coalesce delayed SysTick events instead of replaying zero-time updates
- replace the uncalibrated speed PI loop with deterministic position control
- separate straight and turn profiles and use real-time fault windows
- add raw edge, committed-cycle and accumulator UART diagnostics
