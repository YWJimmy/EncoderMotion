feat(control): add wheel speed PI loop and encoder fault protection

- convert the position profile into per-wheel target encoder speeds
- add independent left and right wheel speed PI controllers
- apply cross-coupled position synchronization to target speeds
- add fixed-point speed filtering and PI anti-windup
- stop both motors when encoder progress is lost under load
- expose target speed, measured speed, PI correction and fault flags in UART logs
- calibrate wheel diameter to 66 mm and encoder CPR to 1469
- extend long-distance motion timeout to 60 seconds
